On evClick ## Event Parameters - pRow( Itemreference )
	# Call the worker wrapper to transform the image
	Do ioImageHandler.$transformImage(ipInitialImage,inScale,inRotation) Returns ipResultImage
	
	Do $cinst.$redraw()